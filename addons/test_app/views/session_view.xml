<odoo>
    <record model="ir.actions.act_window" id="first_test_session_action">
        <field name="name">session.form</field>
        <field name="res_model">test_app.session_model</field>
        <field name="view_type">form</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_smiling_face">Create the first Session</p>
        </field>
    </record>
    <menuitem id="menu_test_session" name="Session List" parent="menu_test" action="first_test_session_action" />

    <!-- record model="ir.ui.view" id="session_tree_view">
        <field name="name">Session List</field>
        <field name="model">test_app.session</field>
        <field name="priority" eval="16" />
        <field name="arch" type="xml">
            <tree string="Session list">
                <field name="name" />
                <field name="start_date" />
                <field name="duration" />
                <field name="seats" />
            </tree>
        </field>
    </record>
    <record model="ir.ui.view" id="session_form_view">
        <field name="name">Session Form</field>
        <field name="model">test_app.session</field>
        <field name="priority" eval="16" />
        <field name="arch" type="xml">
            <form string="Session form">
                <sheet>
                    <group>
                        <field name="name" />
                        <field name="start_date" />
                    </group>
                    <group>
                        <separator string="other" />
                        <field name="duration" />
                        <field name="seats" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    <record model="ir.ui.view" id="session_search_view">
        <field name="name">Session Searc</field>
        <field name="model">test_app.session</field>
        <field name="priority" eval="16" />
        <field name="arch" type="xml">
            <search>
                <field name="name" />
                <field name="date" />
            </search>
        </field>
    </record -->
</odoo>